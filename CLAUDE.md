# Project Instructions

Use `.agents/skills/` for repeatable Shopify workflows. Select the narrowest matching skill; do not preload all skills or standards.

Keep context lean. Inspect only the requested code plus dependencies needed to understand behavior. Load a skill checklist only for deep audits.

For changes, preserve unrelated code and merchant-facing behavior unless the request requires a change. Run relevant existing checks when available and report changed files plus validation results.
