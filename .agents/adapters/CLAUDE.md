# Shopify AI Instructions

Use `.agents/skills/` for repeatable Shopify workflows. Select the narrowest matching skill and do not preload all skills or standards.

Use `.agents/docs/` only when cross-cutting project conventions matter. Keep context lean, inspect only required dependencies, preserve unrelated merchant-facing behavior, and run relevant existing checks after edits when available.
