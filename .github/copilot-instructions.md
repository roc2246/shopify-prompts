# Shopify AI Instructions

Use `.agents/skills/` and select the narrowest matching Shopify skill. Load only its `SKILL.md`; load optional references only when needed.

Prefer Shopify-native theme patterns, small scoped changes, and evidence from inspected files. Preserve merchant customization and unrelated behavior. Run existing checks after edits when available.
